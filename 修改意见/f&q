# WebCross 常见问题（FAQ）

> 依据官网现有文案（产品特性、演示场景、竞品横评、下载页）整理，风格参考 ego browser 与 Browser Use 的 FAQ。

---

## 1. 什么是 WebCross？

WebCross 是一款本地端自主智能体浏览器（Agent Browser）。它基于定制 Chromium 内核构建，让您的 AI 智能体用自然语言接收任务，替您完成繁杂的网页工作——商品价格巡检、批量表单填报、跨平台内容采集与互动等。任务全程后台执行，速度快、Token 省、零干扰，且数据 100% 留在本地。

与传统浏览器不同，WebCross 从内核层就为"智能体驱动"而设计：指令直达渲染进程，而不是在原版 Chrome 之上再包一层 JavaScript 适配。

## 2. WebCross 与 Playwright、Puppeteer 等自动化工具有何不同？

Playwright 和 Puppeteer 是嵌入代码的浏览器自动化库，依赖 CSS 选择器驱动页面，网站一旦调整 DOM，脚本就可能失效，而且每一步都需要人工编写。

WebCross 则采用内核级定制：

- 指令直达渲染进程与浏览器内部协议，中间没有 JS 适配层，省掉 CDP / RPC 往返与序列化开销，同一条任务链路端到端快 3 倍以上；
- 通过 AXTree + SemanticTree 语义化理解页面（详见第 5 条），而不是依赖容易失效的选择器；
- 用自然语言下达任务即可执行，无需编写脚本。

## 3. WebCross 与 Browser Use、Claude for Chrome 等 AI 浏览器方案有何不同？

- **执行环境**：多数同类方案在原版 Chrome 上加一层扩展或适配层，每一步都有往返开销；WebCross 内核直驱页面元素。
- **Token 消耗**：同类方案单任务动辄十万级 Token（Browser Use 约 12–22 万/任务，Perplexity Comet 单任务十万级，Claude for Chrome 每帧 1k+ Token、每步 1–3 秒）；WebCross 借助语义树压缩与 Skill 复用，Token 消耗显著更低，复用后近乎为零。
- **数据主权**：云端方案需要页面内容经第三方服务器中转；WebCross 的凭证、Cookie、执行流水 100% 本地留存。
- **账号安全**：WebCross 支持指纹定制与 IP 池切换，多账号并发互不串号。
- **人机协同**：验证码 / 2FA 毫秒级人工接管，核验完成后从断点恢复，任务链路无需重启。

官网提供了 WebCross 与传统 RPA（UiPath、影刀）、Claude for Chrome、Perplexity Comet、Browser Use、Browserbase 在九个维度上的完整横评。

## 4. 如何开始使用 WebCross？

**在 WebCross Harness（工作台）中使用**

1. **下达初始任务**：在控制面板用自然语言输入您的第一条任务，例如"帮我巡检 Amazon 上 power bank 前 30 名商品的 url、title、price、rating 和前 30 条差评"，智能体即会在后台自主完成打开页面、搜索、点击、抓取、整理并导出到本地等全部步骤。
2. **沉淀 Skill + Workflow**：任务完成后，输入 `/skill-create`，即可从刚才的任务中提炼出专属 Skill 与 Workflow，保存在本地。
3. **调用 Skill**：下次直接输入 `/具体的 Skill 名称`，智能体就会跳过页面探索、自动调用这条已验证的路径直达关键步骤，节省 80% 以上 Token。

**接入第三方 Agent（如 Claude Code、Codex 等）**

WebCross 同样可以接入您日常使用的第三方 Agent。只需在 WebCross 中复制对应的 Skill，粘贴安装到 Claude Code、Codex 等 Agent 的技能目录，安装完成后，即可在第三方 Agent 中直接调用 WebCross 能力，无需集成 SDK。该功能即将上线，敬请期待。

## 5. WebCross 是如何节省 Token 的？

Token 节省来自多层能力的叠加：

- **AXTree 语义压缩**：先把整页压缩成精简的语义骨架，剔除样式、脚本与嵌套容器等 DOM 冗余，上下文体积平均降低 85%；
- **SemanticTree 结构补齐**：在骨架之上补齐控件之间的层级、分组与交互关系——相比只做 AXTree 过滤的传统方案，模型拿到的上下文更短，对页面结构的判断却更精确、信息更丰富；跨域 iframe 与 Shadow DOM 也可直接读取；
- **Skill 复用**：执行过的任务沉淀为 Skill 与 Workflow，下次跳过页面探索直达关键步骤，再节省 80% 以上 Token（详见第 9 条）。

## 6. 智能体运行任务时，我的账号和隐私数据安全吗？

安全。WebCross 是 100% 本地优先架构：

- 账号凭证、Cookie 会话、执行流水与指令，全部留在您的物理机上；
- 任务由本机内核直接执行，页面内容不经任何第三方服务器中转，零云端上传、零数据回传；
- 执行流水本地留存，可审计、可回溯；
- 沉淀的 Skill 与 Workflow 同样仅保存在本地。

## 7. 遇到验证码、2FA 或支付确认等敏感环节怎么办？

WebCross 会在这些环节毫秒级唤起人工接管卡片：任务自动暂停在断点，您完成核验后，智能体立刻从断点恢复后台执行，整条任务链路不需要重启，其他并发实例全程不受影响。接管记录同样只留在本地。

## 8. 我有多个账号需要同时运营，会被平台风控关联吗？

WebCross 为每个实例提供独一无二的指纹环境：

- UA、时区、字体、Canvas 与显卡指纹均可独立配置；
- IP 池随时切换，每个实例一套独立指纹与出口 IP；
- 会话完全隔离，多账号并发执行互不串号；
- 指纹模板可复制、可批量下发，显著降低被风控识别的概率。

无论是多店铺电商运营还是多账号社交媒体管理，都可以放心并发执行。

## 9. 重复性的任务，每次都要从头执行、重新消耗 Token 吗？

不需要。通过 WebCross Harness 执行过的任务，完成后可沉淀为专属 Skill 与 Workflow，保存在本地。下次执行相同或相似任务时直接调用已验证的路径，跳过页面探索、直达关键步骤，节省 80% 以上 Token，并可跨任务复用。例如每天巡检上千个商品链接、批量上架上百个 SKU，都只需要跑通一次。

## 10. WebCross 支持哪些平台？如何收费？

- **平台**：现已开放 macOS 13.0+（支持 Apple Silicon M 系列与 Intel 芯片），DMG 安装包约 128 MB，带 SHA256 签名校验；Windows 11 64 位版本正在架构联调中，即将上线。
- **价格**：公测阶段全功能免费。
- **上手**：下载安装后，按系统提示开启辅助功能与自动化控制权限，在控制面板输入第一条自然语言任务，即可体验端侧智能体的自主执行。
